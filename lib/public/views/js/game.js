/*$(function() {
        function println(data) {
          $('#oldchat').append(username + ' : ' + data + '<br>')
        }
        
        function msg() {
          var data = $('#inchat').val()
          println(data)
          $('#inchat').val('')
        }

        $('#send').bind('click', function(event) {
          msg()
        })
        $('#inchat').keypress(function(event) {
          if(event.keyCode = 13) {
            msg()
          }
        })
      })*/

      // It will not work